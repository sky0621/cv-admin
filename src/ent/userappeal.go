// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/sky0621/cv-admin/src/ent/user"
	"github.com/sky0621/cv-admin/src/ent/userappeal"
)

// UserAppeal is the model entity for the UserAppeal schema.
type UserAppeal struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CreateTime holds the value of the "create_time" field.
	CreateTime time.Time `json:"create_time,omitempty"`
	// UpdateTime holds the value of the "update_time" field.
	UpdateTime time.Time `json:"update_time,omitempty"`
	// Content holds the value of the "content" field.
	Content string `json:"content,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the UserAppealQuery when eager-loading is set.
	Edges        UserAppealEdges `json:"edges"`
	user_id      *int
	selectValues sql.SelectValues
}

// UserAppealEdges holds the relations/edges for other nodes in the graph.
type UserAppealEdges struct {
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [1]bool
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e UserAppealEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*UserAppeal) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case userappeal.FieldID:
			values[i] = new(sql.NullInt64)
		case userappeal.FieldContent:
			values[i] = new(sql.NullString)
		case userappeal.FieldCreateTime, userappeal.FieldUpdateTime:
			values[i] = new(sql.NullTime)
		case userappeal.ForeignKeys[0]: // user_id
			values[i] = new(sql.NullInt64)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the UserAppeal fields.
func (ua *UserAppeal) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case userappeal.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			ua.ID = int(value.Int64)
		case userappeal.FieldCreateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field create_time", values[i])
			} else if value.Valid {
				ua.CreateTime = value.Time
			}
		case userappeal.FieldUpdateTime:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field update_time", values[i])
			} else if value.Valid {
				ua.UpdateTime = value.Time
			}
		case userappeal.FieldContent:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field content", values[i])
			} else if value.Valid {
				ua.Content = value.String
			}
		case userappeal.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for edge-field user_id", value)
			} else if value.Valid {
				ua.user_id = new(int)
				*ua.user_id = int(value.Int64)
			}
		default:
			ua.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the UserAppeal.
// This includes values selected through modifiers, order, etc.
func (ua *UserAppeal) Value(name string) (ent.Value, error) {
	return ua.selectValues.Get(name)
}

// QueryUser queries the "user" edge of the UserAppeal entity.
func (ua *UserAppeal) QueryUser() *UserQuery {
	return NewUserAppealClient(ua.config).QueryUser(ua)
}

// Update returns a builder for updating this UserAppeal.
// Note that you need to call UserAppeal.Unwrap() before calling this method if this UserAppeal
// was returned from a transaction, and the transaction was committed or rolled back.
func (ua *UserAppeal) Update() *UserAppealUpdateOne {
	return NewUserAppealClient(ua.config).UpdateOne(ua)
}

// Unwrap unwraps the UserAppeal entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (ua *UserAppeal) Unwrap() *UserAppeal {
	_tx, ok := ua.config.driver.(*txDriver)
	if !ok {
		panic("ent: UserAppeal is not a transactional entity")
	}
	ua.config.driver = _tx.drv
	return ua
}

// String implements the fmt.Stringer.
func (ua *UserAppeal) String() string {
	var builder strings.Builder
	builder.WriteString("UserAppeal(")
	builder.WriteString(fmt.Sprintf("id=%v, ", ua.ID))
	builder.WriteString("create_time=")
	builder.WriteString(ua.CreateTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("update_time=")
	builder.WriteString(ua.UpdateTime.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("content=")
	builder.WriteString(ua.Content)
	builder.WriteByte(')')
	return builder.String()
}

// UserAppeals is a parsable slice of UserAppeal.
type UserAppeals []*UserAppeal
